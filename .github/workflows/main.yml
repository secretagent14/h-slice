name: Build the game

on:
  pull_request:
    branches: [master]
  push:
    branches: [developmentalist]
  workflow_dispatch:
jobs:
  BuildCaches:
    name: ${{ matrix.name }}
    permissions: write-all
    strategy:
      matrix:
        include:
          - name: Desktop
            os: ubuntu-latest
            setupScript: sh ./setup/unix.sh
            CACHE_NAME: H-Slice.desktop-cache

          - name: Mobile
            os: macos-latest
            setupScript: sh ./setup/mobile.sh
            CACHE_NAME: H-Slice.mobile-cache
    uses: ./.github/workflows/dependencies.yml
    with:
      os: ${{ matrix.os }}
      CACHE_NAME: ${{ matrix.CACHE_NAME }}
      setupScript: ${{ matrix.setupScript }}
  BuildMacos:
    name: macos
    needs: BuildCaches
    permissions: write-all
    uses: ./.github/workflows/mac-universal.yml
    with:
      exportPath: macos
      artifactName: H-Slice_0.2.3_macos
      artifactPath: export/release/macos/bin/*
      cachePath: H-Slice.build-macos
      cacheLibrariesPath: H-Slice.mobile-cache
  BuildDesktop:
    name: ${{ matrix.name }}
    needs: BuildCaches
    permissions: write-all
    strategy:
      matrix:
        include:
          - name: Windows
            os: windows-2022
            buildArgs: windows
            exportPath: windows
            artifactName: H-Slice_0.2.3_windows
            artifactPath: export\release\windows\bin\*
            cachePath: H-Slice.build-windows
            cacheLibrariesPath: H-Slice.desktop-cache
          - name: Linux
            os: ubuntu-22.04
            buildArgs: linux
            exportPath: linux
            artifactName: H-Slice_0.2.3_linux
            artifactPath: export/release/linux/bin/*
            cachePath: H-Slice.build-linux
            cacheLibrariesPath: H-Slice.desktop-cache

    uses: ./.github/workflows/build.yml
    with:
      name: ${{ matrix.name }}
      os: ${{ matrix.os }}
      buildArgs: ${{ matrix.buildArgs }}
      exportPath: ${{ matrix.exportPath }}
      artifactName: ${{ matrix.artifactName }}
      artifactPath: ${{ matrix.artifactPath }}
      cachePath: ${{matrix.cachePath}}
      cacheLibrariesPath: ${{ matrix.cacheLibrariesPath }}

  BuildMobile:
    permissions: write-all
    needs: BuildCaches
    name: ${{ matrix.name }}
    strategy:
      matrix:
        include:
          - name: Android
            os: macos-15
            buildArgs: "android"
            exportPath: android
            artifactName: H-Slice_0.2.3_android
            artifactPath: "export/release/android/bin/app/build/outputs/apk/release/*.apk"
            cachePath: H-Slice.build-android
            cacheLibrariesPath: H-Slice.mobile-cache
          - name: iOS
            os: macos-15
            buildArgs: "ios -nosign"
            exportPath: ios
            artifactName: H-Slice_0.2.3_ios
            artifactPath: "export/release/ios/build/Release-iphoneos/*.ipa"
            cachePath: H-Slice.build-ios
            cacheLibrariesPath: H-Slice.mobile-cache
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      name: ${{ matrix.name }}
      os: ${{ matrix.os }}
      buildArgs: ${{ matrix.buildArgs }}
      exportPath: ${{ matrix.exportPath }}
      artifactName: ${{ matrix.artifactName }}
      artifactPath: ${{ matrix.artifactPath }}
      cachePath: ${{matrix.cachePath}}
      cacheLibrariesPath: ${{ matrix.cacheLibrariesPath }}
